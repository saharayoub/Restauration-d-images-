# Flask
flask>=3.0.0

# PyTorch — CPU-only wheel (small, works everywhere).
# Users with NVIDIA GPU can override by installing the CUDA wheel themselves:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.11.0+cpu
torchvision==0.26.0+cpu

# Image / ML
pillow>=9.0.0
numpy>=1.24.0
requests>=2.28.0
tqdm>=4.65.0
scikit-image>=0.20.0
scipy>=1.10.0
